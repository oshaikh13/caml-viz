// import axios from 'axios';

export function getCodedKGrams(notes: string) {
  // return axios.post('/k-grams', {
  //   notes
  // })

  return {
    then: (callback: Function) => {
      callback([

      ])
    }
  }
}
